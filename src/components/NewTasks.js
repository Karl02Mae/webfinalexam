import React from 'react';
import {
    Box,
    Typography
} from '@mui/material';

export default function NewTasks(props) {
    return (
        <Box>
            <Typography>NEW TASKS</Typography>
        </Box>
    )
}
