const theme = (isDarkMode) => ({
    mode: isDarkMode ? "dark" : "light",
    primary: {
        main: "#a20a0a",

    },
    secondary: {
        main: "#ffa36c",
    },
    error: {
        main: "red",
    },
});


export default theme;